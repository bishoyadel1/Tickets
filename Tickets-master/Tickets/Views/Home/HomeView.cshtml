@* @model IEnumerable<Event>
@{
    Layout = "_Layout";
}
<div class="full-page-bg">
    <div class="container text-center">
        <h1>Welcome to Tazkartk</h1>
        <p>Book your Tickets for upcoming Events now !</p>
    </div>
</div>

<div class="container" style="height:1300px">
    <h1 class="display-3">Latest Events</h1>
    <div class="row">
        <!-- Article 1 -->
        <div class="col-md-4">
            @foreach (var item in Model)
        {
            <div class="card article-card">
                <img src="@item.Image" class="card-img-top article-image" alt="Event Image">
                <div class="card-body">
                    <h5 class="card-title">@item.Name</h5>
                    <p class="card-text">@item.Description</p>
                    <p class="text-muted">on: @item.Date</p>
                    <a href="#" class="btn btn-primary btn-custom">Read More</a>
                </div>
            </div>
        }
            
        </div>

        <!-- Add more articles here -->
    </div>
</div> *@